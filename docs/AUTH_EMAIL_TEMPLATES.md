# RenewTrack Auth Email Templates

Use RenewTrack branding and clear, concise language for confirmation, invite, password reset, magic link, and email-change messages. Never include credentials. Configure redirect URLs for local and production callback, confirmation, and reset-password endpoints in Supabase before enabling each flow.

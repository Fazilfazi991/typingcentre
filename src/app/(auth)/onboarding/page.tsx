"use client";
import { useActionState } from "react"; import { onboardAction } from "../actions";
export default function Onboarding() { const [state, action, pending] = useActionState(onboardAction, { error: "" }); return <main className="auth"><form action={action}><h1>Set up your organization</h1><input name="name" placeholder="Business name" required /><input name="slug" placeholder="organization-slug" required /><input name="location" placeholder="Emirate / location" required /><input name="email" type="email" placeholder="Business email" required /><input name="phone" placeholder="Phone" required /><input name="whatsapp" placeholder="WhatsApp (optional)" /><input name="address" placeholder="Address (optional)" /><input name="primaryColor" defaultValue="#2563EB" required /><label><input name="acceptTerms" type="checkbox" value="true" required /> I accept the Terms and Privacy Policy</label><button disabled={pending}>Create workspace</button>{state.error && <p role="alert">{state.error}</p>}</form></main>; }

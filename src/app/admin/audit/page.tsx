import { adminDate, requirePlatformAdmin } from "@/lib/platform/admin"; export const dynamic="force-dynamic";
export default async function Audit(){const {admin}=await requirePlatformAdmin("/admin/audit");const {data,error}=await admin.from("platform_admin_audit_events").select("id,action,target_type,created_at,profiles(full_name,email),organizations(name)").order("created_at",{ascending:false}).limit(300);if(error)throw error;return <><div className="admin-page-heading"><div><p>Security record</p><h1>Audit log</h1><span>Every platform-side lifecycle, subscription, payment and user access mutation.</span></div></div><section className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Action</th><th>Target</th><th>Typing centre</th><th>Acting admin</th><th>When</th></tr></thead><tbody>{(data??[]).map((e:any)=><tr key={e.id}><td>{e.action}</td><td>{e.target_type}</td><td>{e.organizations?.name??"—"}</td><td>{e.profiles?.full_name??e.profiles?.email??"System"}</td><td>{adminDate(e.created_at)}</td></tr>)}</tbody></table></section></>;}

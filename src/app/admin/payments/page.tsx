import { recordManualPayment } from "@/app/admin/actions"; import { adminDate, requirePlatformAdmin } from "@/lib/platform/admin"; export const dynamic="force-dynamic";
export default async function Payments(){const {admin}=await requirePlatformAdmin("/admin/payments");const [{data:payments},{data:orgs}]=await Promise.all([admin.from("platform_payments").select("*,organizations(name)").order("created_at",{ascending:false}).limit(100),admin.from("organizations").select("id,name").order("name")]);return <><div className="admin-page-heading"><div><p>Billing ledger</p><h1>Payments</h1><span>Manual/offline payments are durable records, never client-side state.</span></div></div><details className="admin-panel"><summary>Record payment</summary><form action={recordManualPayment} className="admin-form compact"><label>Typing centre<select required name="organizationId">{(orgs??[]).map((o:any)=><option value={o.id} key={o.id}>{o.name}</option>)}</select></label><label>Amount<input required type="number" min="0.01" step="0.01" name="amount"/></label><label>Date<input required type="date" name="date"/></label><label>Method<input required name="method" placeholder="Cash, bank transfer…"/></label><label>Reference<input name="reference"/></label><label>Notes<textarea name="notes"/></label><button>Record payment</button></form></details><section className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Typing centre</th><th>Reference</th><th>Amount</th><th>Method</th><th>Payment date</th><th>Status</th></tr></thead><tbody>{(payments??[]).map((p:any)=><tr key={p.id}><td>{p.organizations?.name}</td><td>{p.reference??"—"}</td><td>{p.currency} {p.amount}</td><td>{p.payment_method}</td><td>{adminDate(p.paid_at)}</td><td><span className="admin-badge">{p.status}</span></td></tr>)}</tbody></table></section></>;}

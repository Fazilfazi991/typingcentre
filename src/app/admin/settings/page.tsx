import { savePlatformSettings } from "@/app/admin/actions"; import { requirePlatformAdmin } from "@/lib/platform/admin"; export const dynamic="force-dynamic";
export default async function Settings(){const {admin}=await requirePlatformAdmin("/admin/settings");const [{data:settings},{data:recentCron},{count:failedMessages}]=await Promise.all([admin.from("platform_settings").select("value").eq("key","general").maybeSingle(),admin.from("whatsapp_notifications").select("created_at,status").order("created_at",{ascending:false}).limit(1),admin.from("whatsapp_notifications").select("id",{count:"exact",head:true}).eq("status","failed")]);const value:any=settings?.value??{};return <><div className="admin-page-heading"><div><p>Platform configuration</p><h1>Settings & health</h1><span>Integration secrets are deliberately status-only.</span></div></div><section className="admin-detail-grid"><form action={savePlatformSettings} className="admin-form admin-panel"><h2>General & billing</h2><label>Platform name<input name="platformName" required defaultValue={value.platformName??"Note It"}/></label><label>Support email<input name="supportEmail" required type="email" defaultValue={value.supportEmail??"support@noteitapp.com"}/></label><label>Support phone<input name="supportPhone" defaultValue={value.supportPhone??""}/></label><label>Default currency<input name="currency" required maxLength={3} defaultValue={value.currency??"AED"}/></label><label>Payment notes<textarea name="paymentNotes" defaultValue={value.paymentNotes??""}/></label><button>Save settings</button></form><article className="admin-panel"><h2>System health</h2><dl><dt>Daily digest schedule</dt><dd>10:15 AM (existing schedule)</dd><dt>Most recent digest event</dt><dd>{recentCron?.[0]?.created_at??"No recorded delivery"}</dd><dt>WhatsApp failures</dt><dd>{failedMessages??0}</dd><dt>WhatsApp integration</dt><dd>Configured server-side</dd><dt>Gemini / R2</dt><dd>Status is server-only; no credentials shown</dd></dl></article></section></>;}

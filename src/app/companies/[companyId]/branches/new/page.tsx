import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { createBranchAction } from "@/features/crm/actions";
import { WorkspaceShell } from "@/components/workspace-shell";
import { getWorkspaceContext } from "@/lib/workspace/context";
export default async function NewBranch({params}:{params:Promise<{companyId:string}>}){const context=await getWorkspaceContext();if(!context)redirect("/account-inactive" as never);const {companyId}=await params;const {data:company}=await context.supabase.from("companies").select("id,name,city").eq("id",companyId).maybeSingle();if(!company)notFound();return <WorkspaceShell organizationName={context.organization.name}><header className="page-heading"><Link href={`/companies/${company.id}`}>Back to company</Link><h1>Add branch</h1></header><form action={createBranchAction} className="record-form"><input type="hidden" name="companyId" value={company.id}/><label>Branch name<input name="name" required/></label><label>Emirate<input name="city" defaultValue={company.city} required/></label><label>Branch code<input name="code"/></label><label>Contact person<input name="contactName"/></label><label>Phone<input name="phone"/></label><label>WhatsApp<input name="whatsappNumber"/></label><label>Email<input name="email" type="email"/></label><label>Trade licence number<input name="tradeLicenceNumber"/></label><label className="wide">Address<textarea name="address" rows={3}/></label><label className="wide">Notes<textarea name="notes" rows={3}/></label><button className="primary-button">Create branch</button></form></WorkspaceShell>}

import { redirect } from "next/navigation"; import { resolveAuthDestination } from "@/lib/auth/destination"; import { logoutAction } from "../(auth)/actions";
export const dynamic = "force-dynamic";
export default async function Dashboard() { const destination = await resolveAuthDestination(); if (destination !== "/dashboard") redirect(destination as never); return <main className="workspace"><aside><b>RenewTrack</b><a>Dashboard</a><a>Customers</a><a>Companies</a><a>Documents</a><a>Renewals</a><a>Calendar</a><a>Follow-Ups</a><a>Reports</a><a>Settings</a><form action={logoutAction}><button>Logout</button></form></aside><section><p>Production workspace connected</p><h1>Document Overview</h1><p>Production records will appear after the RLS-protected modules are introduced.</p></section></main>; }

import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { updateFollowUpAction } from "@/features/crm/actions";
import { isSafeUuid } from "@/features/crm/customer-utils";
import { WorkspaceShell } from "@/components/workspace-shell";
import { getWorkspaceContext } from "@/lib/workspace/context";
export const dynamic = "force-dynamic";
export default async function FollowUpEdit({ params }: { params: Promise<{ followUpId: string }> }) { const context=await getWorkspaceContext(); if(!context)redirect("/account-inactive" as never); const {followUpId}=await params; if(!isSafeUuid(followUpId))notFound(); const [{data:followUp},{data:customers},{data:companies}]=await Promise.all([context.supabase.from("follow_ups").select("id,customer_id,company_id,due_at,status,note").eq("id",followUpId).eq("organization_id",context.organization.id).maybeSingle(),context.supabase.from("customers").select("id,full_name").eq("organization_id",context.organization.id).is("archived_at",null),context.supabase.from("companies").select("id,name").eq("organization_id",context.organization.id).is("archived_at",null)]); if(!followUp)notFound(); return <WorkspaceShell organizationName={context.organization.name}><header className="page-heading"><Link href="/follow-ups">Back to follow-ups</Link><h1>Edit follow-up</h1></header><section className="panel">{followUp.status==="completed"?<p className="empty-state">Completed follow-ups are read-only.</p>:<form action={updateFollowUpAction} className="record-form"><input type="hidden" name="followUpId" value={followUp.id}/><input type="hidden" name="returnTo" value="/follow-ups"/><label>Customer<select name="customerId" defaultValue={followUp.customer_id||""}><option value="">No customer</option>{customers?.map(c=><option key={c.id} value={c.id}>{c.full_name}</option>)}</select></label><label>Company<select name="companyId" defaultValue={followUp.company_id||""}><option value="">No company</option>{companies?.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Due date *<input name="dueAt" type="datetime-local" defaultValue={followUp.due_at.slice(0,16)} required/></label><label>Note<input name="note" defaultValue={followUp.note||""}/></label><button className="primary-button">Save changes</button></form>}</section></WorkspaceShell>; }

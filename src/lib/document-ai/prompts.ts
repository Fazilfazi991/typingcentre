export const DOCUMENT_EXTRACTION_INSTRUCTION = `Analyze only the supplied document. Return valid JSON matching the requested schema and no prose. Identify its document type; use other for unfamiliar types while keeping a human-readable document_name. Extract only visibly supported values. Never invent, guess from a name, or infer nationality/company data. Use null for unreadable or unavailable values. Preserve visible spelling where practical. Normalize dates to YYYY-MM-DD. Prefer printed information. Put document-specific values in additional_fields. Add a concise warning for every important unclear value, especially expiry dates.`;
